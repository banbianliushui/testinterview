/**
 * Created by xiaozhu on 2018/4/28.
 */
function _beforeload(options){

    options.beforeload(next);
    function next(){
       var args = Object.prototype.slice(arguments);
        if(args.length>=1){
            if(args[0]){
                // 跳转
            }else{

            }
        }else{

        }
    }
}